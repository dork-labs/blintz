/** The inline-math node's id — kept as a shared constant (schema, command, the
 * edit tooltip's selection test all key off it). Verbatim from Crepe's
 * `latex/constants.ts`. */
export const mathInlineId = "math_inline";
